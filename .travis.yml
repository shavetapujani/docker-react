sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t shavetapujani/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true shavetapujani/docker-react npm run test
